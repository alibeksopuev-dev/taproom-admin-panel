import { Header, CategoriesTable } from '@widgets/categories'
import { Root } from './styled'

export const Categories = () => {
    return (
        <Root>
            <Header />
            <CategoriesTable />
        </Root>
    )
}
